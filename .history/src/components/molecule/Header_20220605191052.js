import React from "react";
import ContentContainer from '../atom/Content'

export default function HeaderContainer() {

    const header = "Chelsea Wylie"
    return (
        <ContentContainer header={header}/>
    )
}